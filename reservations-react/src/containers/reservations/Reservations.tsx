import * as React from 'react';

export interface ReservationsProps {
}

export default class Reservations extends React.Component<ReservationsProps, any> {
  public render() {
    return (
      <div>
        Reservations
      </div>
    );
  }
}
